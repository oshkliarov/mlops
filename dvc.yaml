stages:
  prepare:
    cmd: python src/prepare.py
    deps:
      - src/prepare.py
      - data/raw
    outs:
      - data/prepared

  train:
    cmd: python src/train.py --n_estimators 100 --max_depth 10
    deps:
      - src/train.py
      - data/prepared